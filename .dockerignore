# Git
.git
.gitignore
.github

# Documentation
*.md
!README.md
CNAME
logo.png

# Scripts (except entrypoint.sh which is needed for runtime)
*.sh
!entrypoint.sh

# Development
# NOTE: node_modules is NOT excluded here because builder stage needs to create it
# dist is excluded because we build it from source
dist
.vscode
.idea

# Tests
tests
coverage

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db

# Docker
Dockerfile.patch-old
docker-compose.coolify.yml

# Build artifacts
*.tgz
.npm
.yarn/cache
.yarn/install-state.gz
