# Simple deployment strategy for Coolify
# Use official WAHA image and apply runtime modifications

FROM devlikeapro/waha:latest

# Copy our modified source files
COPY src /app/src

# Rebuild only the necessary parts
WORKDIR /app
RUN yarn build && find ./dist -name "*.d.ts" -delete

# Keep original entrypoint
